class Main {
    Void main() {
        Int c;
        Int abc;
        Int edf;
        abc = 5;
        edf = 123;
        // g = a || b || c && d && gg > 1 && e <= f || g > f;
        // a = a + b - c + d - e + a * b / c * d / e * f;
        abc = new Atb().length().size(abc, edf);
        return;
    }
}

class Atb {
    Int c;
    String d;
    Bool overloadedVar;

    Atb length() {
        Atb overloadedVar;
        overloadedVar = new Atb();
        return overloadedVar;
    }

    Int size(Int x, Int y) {
        Int d;
        return x * c / y - y * c / d + new Atb().length().size(x + y / x * c - new Atb().c, c + new Atb().c) * x + c;
    }
}