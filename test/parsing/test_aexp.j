class TestAExp {
    Void main() {
        TestAExp t;
        t = 1 + -2;
        t = 1 - -2;
        t = 1 + -2 - 3 * -4 / 5;
        t = t + t + t / t + t - 1 * 2;
        t = a.val + b.val + this.val - 1 * null * new TestAExp() >= this.val * -1 || !f || true && 3 < 123456;
    }
}